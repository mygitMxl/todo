"use strict";

var Mock = require('mockjs');

Mock.setup({
  timeout: '1000-4000'
});
Mock.mock('sfas.php', {
  "data|1-2": [{
    // 随机生成1到6个数组元素
    'id|+1': 88,
    // 属性值自动加 1，初始值为88
    '手机|1': ["1", '2'],

    /* 5个随机名字选一个 */
    '质保|18-28': 0,
    // 18至28以内随机整数, 0只是用来确定类型
    '价格|1': ['男', "女"]
  }]
});